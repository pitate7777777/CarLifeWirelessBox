# CarLife 通讯协议深度分析报告

> **版本**: v1.0  
> **日期**: 2026-05-13  
> **分析范围**: CarLifeWirelessBox 项目源码 + 百度 CarLife 官方文档 + 开源项目对比

---

## 目录

1. [协议总览](#1-协议总览)
2. [通道架构](#2-通道架构)
3. [传输层协议格式](#3-传输层协议格式)
4. [CMD 通道握手流程](#4-cmd-通道握手流程)
5. [媒体通道数据格式](#5-媒体通道数据格式)
6. [消息 ID 完整映射表](#6-消息-id-完整映射表)
7. [Protobuf 消息定义](#7-protobuf-消息定义)
8. [本项目实现与标准协议对比](#8-本项目实现与标准协议对比)
9. [版本兼容性分析](#9-版本兼容性分析)
10. [数据流时序图](#10-数据流时序图)
11. [参考来源](#11-参考来源)

---

## 1. 协议总览

CarLife 是百度推出的车机手机互联方案，采用 **6 通道 TCP Socket** 架构，使用 **Protobuf** 作为消息序列化格式。

### 1.1 核心特征

| 特征 | 说明 |
|------|------|
| 传输协议 | TCP Socket（6 个独立连接） |
| 序列化格式 | Protocol Buffers (proto3) |
| 字节序 | Big-Endian（网络字节序） |
| 视频编码 | H.264 (AVC)，可选 H.265 (HEVC) |
| 音频编码 | PCM / AAC / Opus |
| 默认分辨率 | 800×480 (WinCE) / 车机屏幕分辨率 |
| 帧率 | 30fps |
| 认证机制 | 可选，可绕过 |

### 1.2 角色定义

| 角色 | 全称 | 说明 | 典型设备 |
|------|------|------|---------|
| **HU** | Head Unit | 车机端（接收端） | WinCE 车机、本项目的 MdRole |
| **MD** | Mobile Device | 手机端（发送端） | 手机 B (CarWith)、本项目的 HuRole |

> ⚠️ **注意**：本项目中 MdRole 扮演 HU 角色（面向车机），HuRole 扮演 MD 角色（面向手机 B）。命名与标准协议角色相反，因为 MdRole/HuRole 是按"连接方向"命名的。

---

## 2. 通道架构

### 2.1 六通道定义

CarLife 协议定义了 6 个独立的 TCP Socket 通道：

```
┌──────────────────────────────────────────────────────────────┐
│                    CarLife 6 通道架构                         │
├──────────┬──────────┬────────────┬───────────────────────────┤
│ 通道     │ 方向     │ 端口(MD侧)│ 功能                      │
├──────────┼──────────┼────────────┼───────────────────────────┤
│ CMD      │ 双向     │ 7240       │ 控制信令（握手、认证、配置）│
│ VIDEO    │ MD → HU  │ 8240       │ 视频流（H.264 帧）         │
│ MEDIA    │ MD → HU  │ 9240       │ 媒体音频（音乐 PCM/AAC）   │
│ TTS      │ MD → HU  │ 9241       │ 导航语音（TTS 音频）       │
│ VR       │ HU → MD  │ 9242       │ 语音识别（麦克风数据）     │
│ CTRL     │ HU → MD  │ 9340       │ 触摸/按键控制事件          │
└──────────┴──────────┴────────────┴───────────────────────────┘
```

### 2.2 端口映射（ADB Forward 模式）

USB 有线连接时，车机端执行 `adb forward` 建立端口转发：

| 通道 | HU 本地端口 | MD 远程端口 | adb forward 命令 |
|------|------------|------------|-----------------|
| CMD  | 7200 | 7240 | `adb forward tcp:7200 tcp:7240` |
| VIDEO | 8200 | 8240 | `adb forward tcp:8200 tcp:8240` |
| MEDIA | 9200 | 9240 | `adb forward tcp:9200 tcp:9240` |
| TTS  | 9201 | 9241 | `adb forward tcp:9201 tcp:9241` |
| VR   | 9202 | 9242 | `adb forward tcp:9202 tcp:9242` |
| CTRL | 9300 | 9340 | `adb forward tcp:9300 tcp:9340` |

### 2.3 通道类型枚举

```kotlin
enum class ChannelType(val huPort: Int, val mdPort: Int) {
    HU_CMD(7240, 7200),     // CMD 通道
    HU_VIDEO(8240, 8200),   // 视频通道
    HU_MEDIA(9240, 9200),   // 媒体音频通道
    HU_TTS(9241, 9201),     // TTS 通道
    HU_VR(9242, 9202),      // VR 通道
    HU_CTRL(9340, 9300)     // 控制通道
}
```

### 2.4 通道类型编码

```cpp
CMD_CHANNEL   = 1   // 命令通道
VIDEO_CHANNEL = 2   // 视频通道
MEDIA_CHANNEL = 3   // 媒体音频通道
TTS_CHANNEL   = 4   // 导航语音通道
VR_CHANNEL    = 5   // 语音识别通道
CTRL_CHANNEL  = 6   // 触摸控制通道
```

---

## 3. 传输层协议格式

CarLife 协议有两种包头格式：**CMD 格式**（用于 CMD/CTRL 通道）和 **媒体格式**（用于 VIDEO/MEDIA/TTS/VR 通道）。

### 3.1 CMD 通道包头（8 字节）

用于 CMD 和 CTRL 通道的控制消息传输。

```
偏移    字段           大小     说明
────────────────────────────────────────────────
0-1     data_len      2B      Protobuf 数据长度 (Big-Endian)
2-3     reserved      2B      保留字段（值为 0）
4-7     service_type  4B      消息 ID / 服务类型 (Big-Endian)
8+     protobuf_data  变长     Protobuf 序列化数据
```

**字节布局**:
```
+--------+--------+--------+--------+--------+--------+--------+--------+
| data_len (高)   | data_len (低)   | reserved       | service_type    |
|  byte0 |  byte1 |  byte2 |  byte3 |  byte4 |  byte5 |  byte6 |  byte7 |
+--------+--------+--------+--------+--------+--------+--------+--------+
```

**解析代码**:
```kotlin
// 读取
val dataLen = ((header[0].toInt() and 0xFF) shl 8) or (header[1].toInt() and 0xFF)
val serviceType = ((header[4].toInt() and 0xFF) shl 24) or
                  ((header[5].toInt() and 0xFF) shl 16) or
                  ((header[6].toInt() and 0xFF) shl 8) or
                  (header[7].toInt() and 0xFF)

// 写入
header[0] = ((dataLen shr 8) and 0xFF).toByte()
header[1] = (dataLen and 0xFF).toByte()
header[4] = ((serviceType shr 24) and 0xFF).toByte()
header[5] = ((serviceType shr 16) and 0xFF).toByte()
header[6] = ((serviceType shr 8) and 0xFF).toByte()
header[7] = (serviceType and 0xFF).toByte()
```

### 3.2 媒体通道包头（12 字节）

用于 VIDEO、MEDIA、TTS、VR 通道的音视频数据传输。

```
偏移    字段           大小     说明
────────────────────────────────────────────────
0-3     data_len      4B      原始数据长度 (Big-Endian)
4-7     timestamp     4B      时间戳（毫秒）(Big-Endian)
8-11    service_type  4B      消息 ID / 服务类型 (Big-Endian)
12+     raw_data      变长     H.264 帧 / PCM 采样等
```

**字节布局**:
```
+--------+--------+--------+--------+--------+--------+--------+--------+
| data_len                                  | timestamp                   |
|  byte0 |  byte1 |  byte2 |  byte3 |  byte4 |  byte5 |  byte6 |  byte7 |
+--------+--------+--------+--------+--------+--------+--------+--------+
| service_type                              |
|  byte8 |  byte9 |  byte10|  byte11|
+--------+--------+--------+--------+
```

**解析代码**:
```kotlin
val dataLen = ((header[0].toInt() and 0xFF) shl 24) or
              ((header[1].toInt() and 0xFF) shl 16) or
              ((header[2].toInt() and 0xFF) shl 8) or
              (header[3].toInt() and 0xFF)
val timestamp = ((header[4].toInt() and 0xFF) shl 24) or
                ((header[5].toInt() and 0xFF) shl 16) or
                ((header[6].toInt() and 0xFF) shl 8) or
                (header[7].toInt() and 0xFF)
val serviceType = ((header[8].toInt() and 0xFF) shl 24) or
                  ((header[9].toInt() and 0xFF) shl 16) or
                  ((header[10].toInt() and 0xFF) shl 8) or
                  (header[11].toInt() and 0xFF)
```

### 3.3 USB 传输层包头（8 字节，AOA 单流复用模式）

当使用 USB AOA 连接时，6 个通道的数据复用同一条 USB 流，通过额外的 8 字节传输层包头区分通道：

```
偏移    字段           大小     说明
────────────────────────────────────────────────
0-2     reserved      3B      保留（值为 0）
3       msg_type      1B      通道类型 (1=CMD, 2=VIDEO, 3=MEDIA, 6=TOUCH)
4-7     payload_len   4B      消息体长度 (Big-Endian)
```

> ⚠️ 本项目使用 Wi-Fi TCP 连接（非 USB AOA），不使用此包头格式。

### 3.4 ChannelHeader 抽象（本项目实现）

本项目将两种包头统一为 `ChannelHeader` 密封类：

```kotlin
sealed class ChannelHeader {
    companion object {
        const val MAGIC: Short = 0x434C      // "CL" 魔数
        const val CMD_HEADER_SIZE = 8         // CMD 包头大小
        const val MEDIA_HEADER_SIZE = 11      // 媒体包头大小
    }

    // CMD 包头（8 字节）：magic(2) + payloadType(1) + payloadLength(4) + crc(1)
    data class Cmd(val payloadType: Int, val payloadLength: Int, val crc: Byte)

    // 媒体包头（11 字节）：magic(2) + payloadType(1) + timestamp(4) + payloadLength(4)
    data class Media(val payloadType: Int, val timestamp: Int, val payloadLength: Int)
}
```

> ⚠️ **注意**：`ChannelHeader` 是本项目内部的抽象格式（带 `magic` 字段），与标准 CarLife 协议的 CMD 8 字节 / 媒体 12 字节格式不同。实际网络传输使用 `readCarLifeMsg()` / `sendCarLifeMsg()` 方法，采用标准 CarLife 格式。

---

## 4. CMD 通道握手流程

### 4.1 完整握手时序（8 阶段）

```
  HU (车机/转接盒)                              MD (手机 B / CarWith)
    │                                                │
    │══════════ Phase 1: 协议版本 ══════════════════│
    │                                                │
    │── 0x00018001 HU_PROTOCOL_VERSION ────────────►│
    │   {majorVersion=1, minorVersion=0}             │
    │                                                │
    │◄─ 0x00010002 VERSION_MATCH_STATUS ────────────│
    │   {majorVersion=1, minorVersion=0}             │
    │                                                │
    │══════════ Phase 2: 设备信息 ══════════════════│
    │                                                │
    │── 0x00018003 HU_INFO ────────────────────────►│
    │   {deviceType=HEAD_UNIT, osType=ANDROID,       │
    │    manufacturer, model, deviceId, ...}         │
    │                                                │
    │◄─ 0x00010004 MD_INFO ─────────────────────────│
    │   {deviceType=PHONE/BOX, osType=ANDROID,       │
    │    manufacturer, model, deviceId, ...}         │
    │                                                │
    │══════════ Phase 3: 认证请求/响应 ═════════════│
    │                                                │
    │── 0x00018048 HU_AUTHEN_REQUEST ──────────────►│
    │   {method=AUTH_METHOD_NONE, deviceId, ...}     │
    │                                                │
    │◄─ 0x00010049 MD_AUTHEN_RESPONSE ──────────────│
    │   {success=true}                               │
    │                                                │
    │══════════ Phase 4: 认证结果 ══════════════════│
    │                                                │
    │── 0x0001804A HU_AUTHEN_RESULT ───────────────►│
    │   {result=AUTHEN_RESULT_SUCCESS}               │
    │                                                │
    │◄─ 0x0001004B MD_AUTHEN_RESULT ────────────────│
    │   {result=AUTHEN_RESULT_SUCCESS}               │
    │                                                │
    │══════════ Phase 5: 特性配置 ══════════════════│
    │                                                │
    │◄─ 0x00010051 MD_FEATURE_CONFIG_REQUEST ───────│
    │   {videoEnabled=true, audioEnabled=true,       │
    │    touchEnabled=true, musicEnabled=true, ...}  │
    │                                                │
    │── 0x00018052 HU_FEATURE_CONFIG_RESPONSE ──────►│
    │   {videoEnabled=true, audioEnabled=true, ...}  │
    │                                                │
    │══════════ Phase 6: 视频编码器初始化 ══════════│
    │                                                │
    │── 0x00018007 VIDEO_ENCODER_INIT ──────────────►│
    │   {supportedCodecs, preferredCodec,            │
    │    currentResolution, currentFps, bitrateKbps} │
    │                                                │
    │◄─ 0x00010008 VIDEO_ENCODER_INIT_DONE ─────────│
    │   {preferredCodec=H264, resolution=480P, ...}  │
    │                                                │
    │══════════ Phase 7: 开始传输 ══════════════════│
    │                                                │
    │── 0x00018009 VIDEO_ENCODER_START ─────────────►│
    │   {} (空载荷)                                  │
    │                                                │
    │══════════ 数据传输阶段 ═══════════════════════│
    │                                                │
    │◄─ 0x00020001 VIDEO_DATA ──────────────────────│  H.264 帧
    │◄─ 0x00030006 MEDIA_DATA ──────────────────────│  PCM/AAC 音频
    │◄─ 0x00040003 TTS_DATA ────────────────────────│  TTS 音频
    │── 0x00058001 VR_MIC_DATA ────────────────────►│  麦克风数据
    │── 0x00068001 TOUCH_ACTION ───────────────────►│  触摸事件
```

### 4.2 各阶段详细说明

#### Phase 1: 协议版本协商

| 步骤 | 消息 ID | 方向 | Protobuf 消息 | 说明 |
|------|---------|------|--------------|------|
| 1 | 0x00018001 | HU→MD | `CarlifeProtocolVersion` | HU 发送协议版本 (1.0) |
| 2 | 0x00010002 | MD→HU | `CarlifeProtocolVersion` | MD 回复版本匹配 |

```protobuf
message CarlifeProtocolVersion {
    uint32 major_version = 1;  // = 1
    uint32 minor_version = 2;  // = 0
    uint32 patch_version = 3;
    uint64 feature_bitmap = 4;
}
```

#### Phase 2: 设备信息交换

| 步骤 | 消息 ID | 方向 | Protobuf 消息 | 说明 |
|------|---------|------|--------------|------|
| 3 | 0x00018003 | HU→MD | `CarlifeDeviceInfo` | HU 发送车机设备信息 |
| 4 | 0x00010004 | MD→HU | `CarlifeDeviceInfo` | MD 发送手机设备信息 |

```protobuf
message CarlifeDeviceInfo {
    DeviceType device_type = 1;    // HEAD_UNIT / PHONE / BOX
    OsType os_type = 2;            // ANDROID / IOS / LINUX / QNX / WINDOWS
    string os_version = 3;
    string manufacturer = 4;
    string model = 5;
    string device_id = 6;
    string carlife_version = 7;
    string device_name = 8;
    uint32 screen_width = 9;
    uint32 screen_height = 10;
    uint32 screen_dpi = 11;
    string hardware_version = 12;
    repeated string capabilities = 13;
    ScreenInfo screen_info = 14;
    NetworkInfo network_info = 15;
}
```

#### Phase 3-4: 认证流程

| 步骤 | 消息 ID | 方向 | 说明 |
|------|---------|------|------|
| 5 | 0x00018048 | HU→MD | 认证请求 (AUTH_METHOD_NONE) |
| 6 | 0x00010049 | MD→HU | 认证响应 (success=true) |
| 7 | 0x0001804A | HU→MD | HU 认证结果 (SUCCESS) |
| 8 | 0x0001004B | MD→HU | MD 认证结果 (SUCCESS) |

> **认证可绕过**：发送 `AUTH_METHOD_NONE`，双方直接返回 `SUCCESS`。

#### Phase 5: 特性配置

| 步骤 | 消息 ID | 方向 | 说明 |
|------|---------|------|------|
| 9 | 0x00010051 | MD→HU | 特性配置请求 |
| 10 | 0x00018052 | HU→MD | 特性配置响应 |

```protobuf
message CarlifeFeatureConfig {
    int64 feature_bitmap = 1;
    bool video_enabled = 2;
    bool audio_enabled = 3;
    bool tts_enabled = 4;
    bool vr_enabled = 5;
    bool touch_enabled = 6;
    bool gps_enabled = 7;
    bool bluetooth_enabled = 8;
    bool music_enabled = 9;
    bool navigation_enabled = 10;
    bool voice_enabled = 11;
    int32 max_video_bitrate = 12;
    int32 max_audio_bitrate = 13;
    int32 connection_timeout = 14;
}
```

#### Phase 6-7: 视频编码器协商

| 步骤 | 消息 ID | 方向 | 说明 |
|------|---------|------|------|
| 11 | 0x00018007 | HU→MD | 视频编码器初始化（HU 声明能力） |
| 12 | 0x00010008 | MD→HU | 编码器初始化完成（MD 选择参数） |
| 13 | 0x00018009 | HU→MD | 开始视频编码/传输 |

```protobuf
message CarlifeVideoEncoderInfo {
    string encoder_name = 1;
    VideoCodecType codec_type = 2;
    uint32 profile = 3;
    uint32 level = 4;
    repeated VideoResolution supported_resolutions = 5;
    uint32 max_frame_rate = 6;
    uint32 max_bitrate_kbps = 7;
    bool hardware_accelerated = 8;
    uint32 supported_codecs = 9;        // 位掩码
    VideoCodecType preferred_codec = 10;
    uint32 current_resolution = 11;     // 位掩码
    VideoResolution current_resolution_enum = 12;
    uint32 current_fps = 13;
    uint32 bitrate_kbps = 14;
    uint32 i_frame_interval = 15;
    bool hardware_encoder = 16;
}
```

---

## 5. 媒体通道数据格式

### 5.1 VIDEO 通道 (MD → HU)

| 消息 ID | 名称 | 说明 |
|---------|------|------|
| 0x00020001 | VIDEO_DATA | H.264 视频帧数据 |
| 0x00020002 | VIDEO_HEARTBEAT | 视频心跳 |

**数据格式**：12 字节媒体包头 + H.264 NAL 单元

### 5.2 MEDIA 通道 (MD → HU)

| 消息 ID | 名称 | 说明 |
|---------|------|------|
| 0x00030001 | MEDIA_INIT | 音频初始化（采样率、声道、位深） |
| 0x00030002 | MEDIA_STOP | 停止播放 |
| 0x00030003 | MEDIA_PAUSE | 暂停播放 |
| 0x00030004 | MEDIA_RESUME | 恢复播放 |
| 0x00030005 | MEDIA_SEEK_TO | 跳转播放位置 |
| 0x00030006 | MEDIA_DATA | PCM/AAC 音频数据 |

**初始化参数**（MEDIA_INIT）：
```
sampleRate = 48000 Hz
channelConfig = 2 (立体声)
sampleFormat = 16 (16bit)
```

### 5.3 TTS 通道 (MD → HU)

| 消息 ID | 名称 | 说明 |
|---------|------|------|
| 0x00040001 | TTS_INIT | TTS 初始化 |
| 0x00040002 | TTS_STOP | 停止 TTS |
| 0x00040003 | TTS_DATA | TTS 音频数据 |

### 5.4 VR 通道 (HU → MD)

| 消息 ID | 名称 | 方向 | 说明 |
|---------|------|------|------|
| 0x00058001 | VR_MIC_DATA | HU→MD | 车机麦克风语音数据 |
| 0x00050002 | VR_INIT | MD→HU | VR 初始化 |
| 0x00050003 | VR_DATA | MD→HU | VR 识别结果 |
| 0x00050004 | VR_STOP | MD→HU | 停止 VR |

### 5.5 CTRL 通道 (HU → MD)

| 消息 ID | 名称 | 方向 | 说明 |
|---------|------|------|------|
| 0x00068001 | TOUCH_ACTION | HU→MD | 触摸动作 (action, x, y) |
| 0x00068002 | TOUCH_ACTION_DOWN | HU→MD | 触摸按下 |
| 0x00068003 | TOUCH_ACTION_UP | HU→MD | 触摸抬起 |
| 0x00068004 | TOUCH_ACTION_MOVE | HU→MD | 触摸移动 |
| 0x00068005 | TOUCH_SINGLE_CLICK | HU→MD | 单击 |
| 0x00068006 | TOUCH_DOUBLE_CLICK | HU→MD | 双击 |
| 0x00068007 | TOUCH_LONG_PRESS | HU→MD | 长按 |
| 0x00068008 | TOUCH_CAR_HARD_KEY_CODE | HU→MD | 硬按键 |
| 0x00060009 | TOUCH_UI_ACTION_SOUND | MD→HU | UI 操作音 |

---

## 6. 消息 ID 完整映射表

### 6.1 消息 ID 编码规则

```
0x0001xxxx = CMD 通道消息
  0x000180xx = HU(车机) → MD(手机)
  0x000100xx = MD(手机) → HU(车机)
0x0002xxxx = VIDEO 通道消息
0x0003xxxx = MEDIA 通道消息
0x0004xxxx = TTS 通道消息
0x0005xxxx = VR 通道消息
  0x000580xx = HU → MD
0x0006xxxx = CTRL 通道消息
  0x000680xx = HU → MD
```

### 6.2 CMD 通道消息 ID 完整表

| 消息 ID | 常量名 | 方向 | 说明 |
|---------|--------|------|------|
| 0x00018001 | HU_PROTOCOL_VERSION | HU→MD | 协议版本 |
| 0x00010002 | VERSION_MATCH_STATUS | MD→HU | 版本匹配状态 |
| 0x00018003 | HU_INFO | HU→MD | 车机设备信息 |
| 0x00010004 | MD_INFO | MD→HU | 手机设备信息 |
| 0x00018005 | HU_BT_PAIR_INFO | HU→MD | 蓝牙配对信息 |
| 0x00010006 | MD_BT_PAIR_INFO | MD→HU | 蓝牙配对信息 |
| 0x00018007 | VIDEO_ENCODER_INIT | HU→MD | 视频编码器初始化 |
| 0x00010008 | VIDEO_ENCODER_INIT_DONE | MD→HU | 编码器初始化完成 |
| 0x00018009 | VIDEO_ENCODER_START | HU→MD | 开始编码 |
| 0x0001800A | VIDEO_ENCODER_PAUSE | HU→MD | 暂停编码 |
| 0x0001800B | VIDEO_ENCODER_RESET | HU→MD | 重置编码器 |
| 0x0001800C | VIDEO_ENCODER_FRAME_RATE_CHANGE | HU→MD | 帧率变更 |
| 0x0001000D | VIDEO_ENCODER_FRAME_RATE_CHANGE_DONE | MD→HU | 帧率变更完成 |
| 0x0001800E | PAUSE_MEDIA | HU→MD | 暂停媒体 |
| 0x0001800F | CAR_VELOCITY | HU→MD | 车速信息 |
| 0x00018010 | CAR_GPS | HU→MD | GPS 信息 |
| 0x00010018 | SCREEN_ON | MD→HU | 点亮屏幕 |
| 0x0001001B | FOREGROUND | MD→HU | 手机进入前台 |
| 0x00018025 | GO_TO_FOREGROUND | HU→MD | 请求进入前台 |
| 0x00018048 | HU_AUTHEN_REQUEST | HU→MD | 认证请求 |
| 0x00010049 | MD_AUTHEN_RESPONSE | MD→HU | 认证响应 |
| 0x0001804A | HU_AUTHEN_RESULT | HU→MD | HU 认证结果 |
| 0x0001004B | MD_AUTHEN_RESULT | MD→HU | MD 认证结果 |
| 0x00010051 | MD_FEATURE_CONFIG_REQUEST | MD→HU | 特性配置请求 |
| 0x00018052 | HU_FEATURE_CONFIG_RESPONSE | HU→MD | 特性配置响应 |
| 0x00018056 | VIDEO_ENCODER_JPEG | HU→MD | JPEG 编码模式 |

---

## 7. Protobuf 消息定义

### 7.1 核心 Proto 文件清单

| 文件 | 外部类名 | 用途 |
|------|---------|------|
| `carlife_common.proto` | `CarlifeCommonProto` | 通用消息（Header, Status, Timestamp, Version） |
| `CarlifeProtocolVersionProto.proto` | `CarlifeProtocolVersionProto` | 协议版本协商 |
| `carlife_device_info.proto` | `CarlifeDeviceInfoProto` | 设备信息（DeviceType, OsType） |
| `CarlifeAuthenRequestProto.proto` | `CarlifeAuthenRequestProto` | 认证请求 |
| `CarlifeAuthenResponseProto.proto` | `CarlifeAuthenResponseProto` | 认证响应 |
| `CarlifeAuthenResultProto.proto` | `CarlifeAuthenResultProto` | 认证结果 |
| `carlife_auth_method.proto` | `CarlifeAuthMethodProto` | 认证方式枚举 |
| `carlife_feature_config.proto` | `CarlifeFeatureConfigProto` | 特性配置 |
| `carlife_video_encoder_info.proto` | `CarlifeVideoEncoderInfoProto` | 视频编码器信息 |
| `carlife_heartbeat.proto` | `CarlifeHeartbeatProto` | 心跳消息 |
| `carlife_ctrl.proto` | `CarlifeCtrlProto` | 控制消息（触摸、按键、手势） |
| `carlife_video.proto` | `CarlifeVideoProto` | 视频流控制 |
| `carlife_audio.proto` | `CarlifeAudioProto` | 音频流控制 |
| `carlife_tts.proto` | `CarlifeTtsProto` | TTS 消息 |
| `carlife_vr.proto` | `CarlifeVrProto` | VR 消息 |
| `CarlifeMusicInitProto.proto` | `CarlifeMusicInitProto` | 音乐初始化 |
| `carlife_status.proto` | `CarlifeStatusProto` | 连接状态 |
| `CarlifeConnectRequestProto.proto` | `CarlifeConnectRequestProto` | 连接请求 |
| `CarlifeConnectResponseProto.proto` | `CarlifeConnectResponseProto` | 连接响应 |
| `CarlifeDisconnectRequestProto.proto` | `CarlifeDisconnectRequestProto` | 断开请求 |
| `CarlifeDisconnectResponseProto.proto` | `CarlifeDisconnectResponseProto` | 断开响应 |
| `CarlifeRegisterRequestProto.proto` | `CarlifeRegisterRequestProto` | 注册请求 |
| `CarlifeRegisterResponseProto.proto` | `CarlifeRegisterResponseProto` | 注册响应 |
| `carlife_register_type.proto` | `CarlifeRegisterTypeProto` | 注册类型枚举 |
| `carlife_version.proto` | `CarlifeVersionProto` | 版本协商 |
| `carlife_cmd.proto` | `CarlifeCmdProto` | 通用命令消息 |
| `carlife_video_frame.proto` | `CarlifeVideoFrameProto` | 视频帧数据 |
| `carlife_audio_frame.proto` | `CarlifeAudioFrameProto` | 音频帧数据 |

### 7.2 关键枚举定义

```protobuf
// 设备类型
enum DeviceType {
    DEVICE_TYPE_UNKNOWN = 0;
    DEVICE_TYPE_PHONE = 1;
    DEVICE_TYPE_HEAD_UNIT = 2;
    DEVICE_TYPE_BOX = 3;
}

// 操作系统
enum OsType {
    OS_UNKNOWN = 0;
    OS_ANDROID = 1;
    OS_IOS = 2;
    OS_LINUX = 3;
    OS_QNX = 4;
    OS_WINDOWS = 5;
}

// 认证方式
enum AuthMethod {
    AUTH_METHOD_NONE = 0;
    AUTH_METHOD_TOKEN = 1;
    AUTH_METHOD_CERTIFICATE = 2;
}

// 认证结果
enum AuthenResultCode {
    AUTHEN_RESULT_UNKNOWN = 0;
    AUTHEN_RESULT_SUCCESS = 1;
    AUTHEN_RESULT_FAILED = 2;
    AUTHEN_RESULT_TIMEOUT = 3;
    AUTHEN_RESULT_CANCELED = 4;
    AUTHEN_RESULT_UNSUPPORTED = 5;
}

// 视频编解码器
enum VideoCodecType {
    VIDEO_CODEC_UNKNOWN = 0;
    VIDEO_CODEC_H264 = 1;
    VIDEO_CODEC_H265 = 2;
    VIDEO_CODEC_VP8 = 3;
    VIDEO_CODEC_VP9 = 4;
}

// 视频分辨率
enum VideoResolution {
    RES_UNKNOWN = 0;
    RES_480P = 1;
    RES_720P = 2;
    RES_1080P = 3;
    RES_2K = 4;
    RES_4K = 5;
}
```

---

## 8. 本项目实现与标准协议对比

### 8.1 对比总表

| 对比项 | 标准 CarLife 协议 | 本项目实现 | 差异说明 |
|--------|-------------------|-----------|---------|
| **通道数** | 6 个 TCP 通道 | 6 个 TCP 通道 | ✅ 完全一致 |
| **端口分配** | MD: 7240/8240/9240/9241/9242/9340 | 同上 + HU: 7200/8200/9200/9201/9202/9300 | ✅ 一致 |
| **CMD 包头** | 8 字节 (data_len 2B + reserved 2B + service_type 4B) | 同上 | ✅ 一致 |
| **媒体包头** | 12 字节 (data_len 4B + timestamp 4B + service_type 4B) | 同上 | ✅ 一致 |
| **握手流程** | 8 阶段（版本→设备→认证→特性→编码器→开始） | 8 阶段 | ✅ 一致 |
| **协议版本** | 1.0 (major=1, minor=0) | 1.0 | ✅ 一致 |
| **认证方式** | 可选，可绕过 | AUTH_METHOD_NONE，直接返回成功 | ✅ 一致 |
| **视频编码** | H.264, 800×480, 30fps | H.264, 可配置分辨率和帧率 | ✅ 兼容 |
| **音频格式** | PCM 48kHz/16bit/立体声 | PCM 48kHz/16bit/立体声 | ✅ 一致 |
| **消息 ID** | 标准编码 (0x000180xx/0x000100xx) | 同上 | ✅ 一致 |
| **Protobuf** | proto2/proto3 | proto3 (protobuf-javalite) | ✅ 兼容 |
| **心跳** | 3 秒间隔 | 3 秒间隔 | ✅ 一致 |
| **ChannelHeader** | 无 magic 字段 | 内部抽象有 magic (0x434C)，网络传输无 magic | ⚠️ 内部格式不同，网络格式一致 |

### 8.2 消息 ID 对比

本项目 `HuRole.CarLifeMsg` 定义的消息 ID 与标准协议完全一致：

```kotlin
object CarLifeMsg {
    // HU → MD
    const val HU_PROTOCOL_VERSION        = 0x00018001  // ✅ 一致
    const val HU_INFO                    = 0x00018003  // ✅ 一致
    const val VIDEO_ENCODER_INIT         = 0x00018007  // ✅ 一致
    const val VIDEO_ENCODER_START        = 0x00018009  // ✅ 一致
    const val VIDEO_ENCODER_PAUSE        = 0x0001800A  // ✅ 一致
    const val VIDEO_ENCODER_RESET        = 0x0001800B  // ✅ 一致
    const val HU_AUTHEN_REQUEST          = 0x00018048  // ✅ 一致
    const val HU_AUTHEN_RESULT           = 0x0001804A  // ✅ 一致
    const val HU_FEATURE_CONFIG_RESPONSE = 0x00018052  // ✅ 一致

    // MD → HU
    const val VERSION_MATCH_STATUS       = 0x00010002  // ✅ 一致
    const val MD_INFO                    = 0x00010004  // ✅ 一致
    const val VIDEO_ENCODER_INIT_DONE    = 0x00010008  // ✅ 一致
    const val MD_AUTHEN_RESPONSE         = 0x00010049  // ✅ 一致
    const val MD_AUTHEN_RESULT           = 0x0001004B  // ✅ 一致
    const val MD_FEATURE_CONFIG_REQUEST  = 0x00010051  // ✅ 一致

    // VIDEO
    const val VIDEO_DATA                 = 0x00020001  // ✅ 一致
    const val VIDEO_HEARTBEAT            = 0x00020002  // ✅ 一致

    // MEDIA
    const val MEDIA_INIT                 = 0x00030001  // ✅ 一致
    const val MEDIA_STOP                 = 0x00030002  // ✅ 一致
    const val MEDIA_PAUSE                = 0x00030003  // ✅ 一致
    const val MEDIA_RESUME               = 0x00030004  // ✅ 一致
    const val MEDIA_SEEK_TO              = 0x00030005  // ✅ 一致
    const val MEDIA_DATA                 = 0x00030006  // ✅ 一致

    // TTS
    const val TTS_INIT                   = 0x00040001  // ✅ 一致
    const val TTS_STOP                   = 0x00040002  // ✅ 一致
    const val TTS_DATA                   = 0x00040003  // ✅ 一致

    // VR
    const val VR_MIC_DATA                = 0x00058001  // ✅ 一致
    const val VR_INIT                    = 0x00050002  // ✅ 一致
    const val VR_DATA                    = 0x00050003  // ✅ 一致
    const val VR_STOP                    = 0x00050004  // ✅ 一致

    // CTRL
    const val TOUCH_ACTION               = 0x00068001  // ✅ 一致
    const val TOUCH_ACTION_DOWN          = 0x00068002  // ✅ 一致
    const val TOUCH_ACTION_UP            = 0x00068003  // ✅ 一致
    const val TOUCH_ACTION_MOVE          = 0x00068004  // ✅ 一致
    const val TOUCH_SINGLE_CLICK         = 0x00068005  // ✅ 一致
    const val TOUCH_DOUBLE_CLICK         = 0x00068006  // ✅ 一致
    const val TOUCH_LONG_PRESS           = 0x00068007  // ✅ 一致
    const val TOUCH_CAR_HARD_KEY_CODE    = 0x00068008  // ✅ 一致
}
```

### 8.3 与百度官方 CarLifeVehicleLib 对比

| 对比项 | CarLifeVehicleLib (C++) | 本项目 (Kotlin) | 说明 |
|--------|------------------------|-----------------|------|
| 语言 | C++ | Kotlin | 跨语言 |
| 平台 | Android/Linux/WinCE/QNX | Android | 范围更小 |
| USB 通信 | ADB Forward + TCP | Wi-Fi TCP / USB Tethering TCP | 本项目更灵活 |
| 认证 | 可选，可绕过 | 可绕过 (AUTH_METHOD_NONE) | ✅ 一致 |
| 视频解码 | 不含解码器（需上层实现） | 不含解码器（透传） | ✅ 一致 |
| 通道管理 | 6 个独立 Socket | 6 个独立 Channel | ✅ 架构一致 |

### 8.4 与 CarProjection (Java) 对比

| 对比项 | CarProjection | 本项目 | 说明 |
|--------|--------------|--------|------|
| 连接方式 | USB AOA (单流复用) | Wi-Fi TCP (6 独立连接) | 本项目更适合无线场景 |
| 包头格式 | 8 字节 USB 传输头 + CarLife 包头 | 直接 CarLife 包头 | 本项目无 USB 复用层 |
| 视频编码 | MediaProjection + H.264 | 同上 | ✅ 一致 |
| 音频捕获 | AudioPlaybackCapture (Android 10+) | 同上 | ✅ 一致 |
| 触摸注入 | AccessibilityService | 同上 | ✅ 一致 |
| 认证 | 直接返回 true | AUTH_METHOD_NONE + 直接返回成功 | ✅ 一致 |

---

## 9. 版本兼容性分析

### 9.1 CarLife 版本分界点

| CarLife 版本 | 协议版本 | WinCE 兼容性 | 说明 |
|-------------|---------|-------------|------|
| ≤ 6.7.2 | 1.0 | ✅ 完全兼容 | 最后一个兼容 WinCE 的版本 |
| ≥ 7.2 | 可能 2.0 | ❌ 黑屏/断连 | 协议变更，不兼容旧车机 |

### 9.2 版本检测逻辑

```kotlin
object VersionDetector {
    fun needTranslation(major: Int, minor: Int): Boolean {
        return major > 6 || (major == 6 && minor > 7)
    }
}
```

### 9.3 协议转换需求

当高版本 CarLife (7.2+) 与旧版 WinCE 车机不兼容时，需要协议转换：

```kotlin
object ProtocolTranslator {
    // 强制降级到协议版本 1.0
    fun translateProtocolVersion(major: Int, minor: Int): Pair<Int, Int> {
        return Pair(1, 0)
    }

    // 强制视频参数：800×480, 30fps
    fun translateVideoConfig(width: Int, height: Int, frameRate: Int): Triple<Int, Int, Int> {
        return Triple(800, 480, 30)
    }

    // H.265 → H.264 转换（当前为透传模式）
    fun translateVideoFrame(frame: ByteArray, codecType: Int): Pair<ByteArray, Int>
}
```

---

## 10. 数据流时序图

### 10.1 完整数据流

```
[手机B (CarWith)]                    [旧手机 (转接盒)]                    [WinCE 车机]
      │                                      │                                  │
      │  Wi-Fi TCP (6通道)                    │  USB Tethering TCP (6通道)        │
      │  7240/8240/9240                       │  7200/8200/9200                  │
      │  9241/9242/9340                       │  9201/9202/9300                  │
      │                                      │                                  │
      │◄════ HuRole (HU) ═════►│◄═══════ MdRole (MD) ═══════►│
      │     连接手机B              │        监听车机连接            │
      │                                      │                                  │
      │── 握手 (Phase 1-7) ──►│              │                                  │
      │                                      │◄── 握手 (Phase 1-7) ──│
      │                                      │                                  │
      │═══════════════ 数据传输 ═════════════════════════════════════│
      │                                      │                                  │
      │── VIDEO_DATA (H.264) ──►│── 转发 ──►│── VIDEO_DATA ──────────►│
      │── MEDIA_DATA (PCM) ────►│── 转发 ──►│── MEDIA_DATA ──────────►│
      │── TTS_DATA ────────────►│── 转发 ──►│── TTS_DATA ────────────►│
      │                                      │                                  │
      │◄─ VR_MIC_DATA ─────────│◄── 转发 ◄─│◄─ VR_MIC_DATA ─────────│
      │◄─ TOUCH_ACTION ────────│◄── 转发 ◄─│◄─ TOUCH_ACTION ────────│
```

### 10.2 ConnectionService 协调流程

```
ConnectionService
    │
    ├── startMdRole()
    │   ├── 启动 6 个 TcpServer (7200/8200/9200/9201/9202/9300)
    │   ├── mDNS 广播 (_carlife._tcp.)
    │   └── 等待车机连接
    │
    ├── startHuRole()
    │   ├── 创建 6 个 Channel 连接手机 B
    │   ├── 完成 CarLife 握手 (Phase 1-7)
    │   └── HuState → CONNECTED
    │
    ├── tryStartVideoAndAudioServices()
    │   ├── 检查 MdRole.READY && HuRole.CONNECTED
    │   ├── 请求 MediaProjection 授权
    │   ├── 启动 VideoService (MediaProjection + H.264 编码)
    │   ├── 启动 AudioService (AudioPlaybackCapture + AAC)
    │   └── 启动 DynamicBitrate (动态码率调节)
    │
    └── 数据转发
        ├── 手机 B VIDEO → MdRole.sendData(MD_VIDEO) → 车机
        ├── 手机 B MEDIA → MdRole.sendData(MD_MEDIA) → 车机
        ├── 车机 CTRL → HuRole.sendControlMsg() → 手机 B
        └── 车机 VR → HuRole.sendVrData() → 手机 B
```

---

## 11. 参考来源

### 11.1 项目内文档

| 文档 | 路径 | 说明 |
|------|------|------|
| 架构文档 | `docs/ARCHITECTURE.md` | 系统架构、模块依赖、数据流 |
| 技术文档 | `docs/项目技术文档.md` | CarLife 协议深度调研 |
| 参考 APP 分析 | `docs/参考app分析.md` | 百度 CarLife 车机端 APK 分析 |
| PRD | `docs/PRD.md` | 产品需求文档 |

### 11.2 开源项目参考

| 项目 | 仓库 | 参考价值 |
|------|------|---------|
| CarLifeVehicleLib | [ApolloAuto/apollo-DuerOS](https://github.com/ApolloAuto/apollo-DuerOS/tree/master/CarLifeVehicleLib) | 协议规范权威参考（C++） |
| CarProjection | [aa112901/CarProjection](https://github.com/aa112901/CarProjection) | 手机端协议实现（Java, USB AOA） |
| ICar | [yenole/icar](https://github.com/yenole/icar) | 架构参考（VirtualDisplay） |
| WirelessAndroidAutoDongle | [nisargjhaveri/WirelessAndroidAutoDongle](https://github.com/nisargjhaveri/WirelessAndroidAutoDongle) | USB+Wi-Fi 桥接架构 |

### 11.3 官方文档

| 来源 | 说明 |
|------|------|
| 百度 CarLife 车机手机互联方案接入详细指南 V2.1.1.pdf | 官方接入文档（项目 docs/ 目录内） |
| 百度 CarLife 动态链接库使用说明.docx | 官方 SDK 文档（项目 docs/ 目录内） |

---

## 附录 A: 心跳机制

| 参数 | 值 | 说明 |
|------|-----|------|
| 心跳间隔 | 3000ms | 每 3 秒发送一次 |
| 心跳超时 | 9000ms | 3 次未收到判定断开 |
| 心跳消息 | CMD 通道 Protobuf | 使用 `CarlifeHeartbeat` 消息 |

## 附录 B: 错误码

| 消息 ID | 名称 | 说明 |
|---------|------|------|
| 0x00010002 | VERSION_MATCH_STATUS | 版本不匹配时断开 |
| AUTHEN_RESULT_FAILED | 认证失败 | 认证不通过 |
| CONNECT_RESULT_TIMEOUT | 连接超时 | 网络异常 |
| CONNECT_RESULT_REJECTED | 连接被拒 | 设备未授权 |

## 附录 C: 缓冲区大小建议

| 通道 | 包头大小 | 建议缓冲区 |
|------|---------|-----------|
| CMD  | 8 字节 | 40 KB |
| VIDEO | 12 字节 | 600 KB |
| MEDIA | 12 字节 | 100 KB |
| TTS  | 12 字节 | 50 KB |
| VR   | 12 字节 | 50 KB |
| CTRL | 8 字节 | 10 KB |
